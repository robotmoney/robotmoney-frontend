{
  "//": "Deny by default. Maps GitHub login (lowercase) -> permitted path globs per operation. Operations: create | edit | delete. 'all' is shorthand granting create+edit+delete for its globs. '*' is the baseline unioned into EVERY author, including logins not otherwise listed. An author with no matching glob for an operation on a path is BLOCKED. Globs use Bun.Glob syntax.",
  "users": {
    "lucky-tensor": { "all": ["**"] },
    "lextothex": { 
      "edit":   ["frontend/**", "docs/**"],
      "create": ["brand-assets/**", "docs/marketing/**"],
      "delete": ["brand-assets/**", "docs/marketing/**"]  
    },
    "fernandezdavid": {
      "edit":   ["frontend/**", "docs/**"],
      "create": ["brand-assets/**", "docs/marketing/**"],
      "delete": ["brand-assets/**", "docs/marketing/**"]
    },
    "*": { "create": [], "edit": [], "delete": [] }
  }
}
