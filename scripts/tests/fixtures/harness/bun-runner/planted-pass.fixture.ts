// See planted-fail.fixture.ts's header: explicit-path-only, never auto-collected.
import { expect, test } from "bun:test";

test("planted pass — the control for planted-fail.fixture.ts", () => {
  expect(1).toBe(1);
});
