# Robot Money — https://robotmoney.network
# Autonomous treasury for the agent economy.

User-agent: *
Allow: /
# Operator dashboards are not for indexing.
Disallow: /admin
Disallow: /admin/

# AI / LLM crawlers are welcome — the site content is public and meant to be cited.
User-agent: GPTBot
Allow: /
User-agent: OAI-SearchBot
Allow: /
User-agent: ClaudeBot
Allow: /
User-agent: anthropic-ai
Allow: /
User-agent: PerplexityBot
Allow: /
User-agent: Google-Extended
Allow: /

# The SPA fallback shell backend/src/api/static.ts serves for client routes it
# cannot prerender. No content of its own; not a page.
Disallow: /_shell.html

# This site renders client-side, so fetching a page returns the shell and none
# of the data. The two files below are where the content actually is:
#   /llms.txt      site index plus every public JSON endpoint, absolute URLs
#   /openapi.json  the callable description of that API, no key required
# Comments are not a crawl directive, they are a signpost for whoever reads this
# file looking for a way in.

Sitemap: https://robotmoney.network/sitemap.xml
