node_modules/
.env
.env.local
*.log
.DS_Store
dist/
playwright-report/
test-results/

# Cloudflare Pages deploy dir assembled by scripts/cloudflare-statics.sh.
_site/

# Standing local demo state (written by `bun run demo`, removed by `demo:down`).
.agents/demo-state.json

# Onboarding-eval output (written by `bun run eval:onboarding`; the nightly
# uploads them as artifacts — they are run RESULTS, never repo content).
.agents/onboarding-eval-scorecard.json
.agents/onboarding-eval-transcripts/
